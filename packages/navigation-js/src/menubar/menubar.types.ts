/**
 * Options for generating menubar item CSS class names.
 */
export interface MenubarItemClassesOptions {
  /** Whether this item is the currently active/expanded item. @default false */
  active?: boolean
  /** Whether this item is in a disabled, non-interactive state. @default false */
  disabled?: boolean
}
