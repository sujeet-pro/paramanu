export interface CenterClassesOptions {
  /** Render as inline-flex instead of flex. */
  inline?: boolean
  /** Also center text content with text-align. */
  textCenter?: boolean
}

export interface CenterProps extends CenterClassesOptions {}
