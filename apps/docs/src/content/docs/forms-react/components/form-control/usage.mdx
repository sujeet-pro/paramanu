---
title: "Form Control"
description: "A wrapper that groups a label, input, and helper or error text."
badge:
  text: In Development
  variant: caution
sidebar:
  label: "Form Control"
---

import ComponentPreview from "../../../../../components/ComponentPreview.astro"
import { Aside } from "@astrojs/starlight/components"

<Aside type="caution" title="In Development">
  This component is currently in development. The API is not yet stable and may change in future
  releases.
</Aside>

The Form Control groups a label, input, and helper/error text into a single accessible unit.

## Basic Usage

<ComponentPreview
  htmlCode={`<div class="pm-form-ctrl pm-form-ctrl--vertical" role="group">...</div>`}
  reactCode={`import { FormCtrl } from "@paramanu/forms-react"\n\nexport default function App() {\n  return (\n    <FormCtrl helperText="Enter your name">\n      <label>Name</label>\n      <input />\n    </FormCtrl>\n  )\n}`}
/>
