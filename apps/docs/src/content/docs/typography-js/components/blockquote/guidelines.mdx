---
title: "Blockquote"
description: "A styled blockquote for quotations with optional citation, accent color, and decorative icon."
badge:
  text: In Development
  variant: caution
sidebar:
  hidden: true
---

## Accessibility

Uses the semantic `<blockquote>` element. The `cite` prop renders a `<cite>` child for attribution. The `citeUrl` prop sets the HTML `cite` attribute.
