---
title: "Inline Message"
description: "Compact feedback message for form fields and inline content areas."
badge:
  text: In Development
  variant: caution
sidebar:
  hidden: true
---

## Accessibility

- Uses `role="alert"` for warning/danger, `role="status"` for info/success
- Use `sm` size for form field hints

## Best Practices

- Use `danger` for validation errors, `success` for valid inputs
- Keep messages concise and actionable
- Place directly below the related form field
