---
title: "Avatar"
description: "A visual representation of a user or entity with image, initials, or icon fallback."
badge:
  text: In Development
  variant: caution
sidebar:
  hidden: true
---

## Accessibility

- Uses `role="img"` with `aria-label` for screen readers.
- Img avatars include meaningful `alt` text.

## Best Practices

- Use `circle` for user profiles, `square` for organizations or apps.
- Provide a `name` prop for automatic initials generation.
