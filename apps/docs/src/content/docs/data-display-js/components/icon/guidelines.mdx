---
title: "Icon"
description: "A wrapper for SVG icons with size, color, and spin animation support."
badge:
  text: In Development
  variant: caution
sidebar:
  hidden: true
---

## Accessibility

- Provide `label` for meaningful icons (sets `role="img"` and `aria-label`).
- Omit `label` for decorative icons (sets `aria-hidden="true"`).
