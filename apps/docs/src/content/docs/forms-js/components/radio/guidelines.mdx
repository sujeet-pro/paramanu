---
title: "Radio"
description: "A radio button for selecting a single option from a group of choices."
badge:
  text: In Development
  variant: caution
sidebar:
  hidden: true
---

## Accessibility

| Feature             | Detail                               |
| ------------------- | ------------------------------------ |
| `role="radiogroup"` | Group container uses radiogroup role |
| `aria-invalid`      | Applied when `invalid` is true       |
| Keyboard            | Arrow keys navigate between radios   |

## Best Practices

- Always group radios using RadioGroup with a shared `name`.
- Use radio buttons when only one option can be selected.
