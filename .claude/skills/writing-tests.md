---
description: Apply when writing unit tests, accessibility tests, or React component tests. Auto-use when creating or modifying *.test.ts, *.a11y.test.ts, or *.test.tsx files, or when the user asks to write tests for a component.
---

# Writing Tests

Read and follow `agents-guidelines/writing-tests.md`.

Covers: class builder tests, accessibility tests (JSDOM + axe-core), React component tests, query priorities, portable stories, coverage requirements.
